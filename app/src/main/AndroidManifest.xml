<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="app.christopher.shape_android_open_assignment">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/puppy"
        android:name=".DogsApplication"
        android:label="@string/app_name"
        android:roundIcon="@drawable/puppy"
        android:supportsRtl="true"
        android:theme="@style/Theme.ShapeAndroidOpenAssignment">
        <activity
            android:name=".features.main.MainActivity"
            android:screenOrientation="fullSensor"
            android:configChanges="orientation|navigation|uiMode|screenSize|touchscreen|smallestScreenSize"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>